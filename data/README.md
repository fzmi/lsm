# Dataset

After downloading the LSM dataset, place or symlink the extracted dataset at `data/lsm`.

## Expected Structure

```
lsm
├── brisbane
│   └── image.tiff
├── camarillo
│   └── image.tiff
...
```
